services:
  tallo-test:
    # Build from the local Dockerfile in the current directory
    build: .
    container_name: tallo-local-test
    
    # Map port 3000 (container) to 5521 (host)
    ports:
      - "5521:3000"
      
    # FIX: Mount to /data because that is where the App is configured to write
    volumes:
      - ./test-data:/data
      
    environment:
      - NODE_ENV=production
      - PORT=3000
      
    restart: no